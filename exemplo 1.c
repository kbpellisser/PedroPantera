#include <stdio.h>

main (){

int n = 0;
printf ("\nValor: %d", n);
n++; //Incrementando uma unidade
printf ("\nValor: %d", n);
n = n+1;
printf ("\nValor: %d", n);
n = n+2;
printf ("\nValor: %d", n);

//decremento

n--;
printf ("\nValor: %d", n)
n -= 2;
}